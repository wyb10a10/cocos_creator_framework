import { _decorator } from "cc";
import { UIView } from "../../ui/UIView";

const {ccclass} = _decorator;

@ccclass
export default class UINotice extends UIView {
    
    public init() {

    }
}
